<UserControl x:Class="EliteInfoPanel.Controls.SummaryCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:EliteInfoPanel.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <StackPanel>
        <TextBlock Text="{Binding Title}" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Foreground="Orange" 
                   Margin="0,0,0,5"/>

        <ItemsControl ItemsSource="{Binding Items}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="0,3,0,3">
                        <materialDesign:PackIcon Kind="{Binding Icon}" 
                                                 Width="24" 
                                                 Height="24"
                                                 VerticalAlignment="Center"
                                                 Margin="0,0,6,0" 
                                                 Foreground="{Binding Foreground}"
                                                 Visibility="{Binding Icon, Converter={StaticResource BoolToVis}}"/>
                        <TextBlock Text="{Binding Content}" 
                                   FontSize="20" 
                                   Foreground="{Binding Foreground}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Fuel Bar - Simple Implementation -->
        <StackPanel Visibility="{Binding ShowFuelBar, Converter={StaticResource BoolToVis}}"
                    Margin="0,10,0,0">
            <TextBlock Foreground="{DynamicResource MaterialDesignBody}"
                       FontSize="26">
                <TextBlock.Text>
                    <MultiBinding StringFormat="Fuel: Main {0:0.00} &#10;Reserve {1:0.00}">
                        <Binding Path="FuelMain" />
                        <Binding Path="FuelReservoir" />
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>

            <!-- Simple Progress Bar for Fuel -->
            <ProgressBar Height="28" 
                         Margin="0,4,0,0" 
                         Value="{Binding FuelBarRatio}" 
                         
                         Maximum="1.0" 
                         Foreground="Orange" 
                         Background="DarkSlateGray"/>
        </StackPanel>
    </StackPanel>
</UserControl>